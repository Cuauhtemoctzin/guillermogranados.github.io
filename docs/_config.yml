theme: jekyll-theme-time-machine

title: The Series Analyzer
description: Welcome to my personal page where I share and showcase my projects and ideas.

defaults:
  -
    scope:
      path: "" # an empty string here means all files in the project
      type: "posts" # previously `post` in Jekyll 2.2.
    values:
      layout: "default"
